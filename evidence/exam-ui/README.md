# Exam UI evidence

These screenshots are generated by `tests/e2e/exam-ui.spec.ts` from deterministic standalone data.

- `readiness-desktop.png`: the rules step and monitoring disclosure.
- `current-question-desktop.png`: the connected one-question shell with progress and elapsed time.
- `submitted-mobile-reduced-motion.png`: the submitted result at a 390 by 844 viewport with reduced motion enabled.
- `integrity-review-desktop.png`: factual integrity-review language and the appeal entry.

The same check verifies keyboard focus on the rules checkbox before starting the exam.
